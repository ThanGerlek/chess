package services;

import http.CreateGameRequest;
import http.CreateGameResponse;

/**
 * Provides the Create New Game service, which registers and initializes a new empty game.
 */
public class CreateGameService {

    /**
     * Creates a new game from the given {@link http.CreateGameRequest}.
     *
     * @param request a {@code CreateGameRequest} representing the HTTP request.
     * @return a {@link http.CreateGameResponse} representing the resulting HTTP response.
     */
    public CreateGameResponse createGame(CreateGameRequest request) {
        return null;
    }

}
